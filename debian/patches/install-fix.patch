Index: thermod/setup.py
===================================================================
--- thermod.orig/setup.py
+++ thermod/setup.py
@@ -36,13 +36,13 @@ setup(name='thermod',
       url='TODO',
       license = 'GPL-3.0+',
       packages=['thermod'],
-      scripts=['bin/thermod'],
+      #scripts=['bin/thermod'],
       install_requires=['jsonschema >= 2.3.0',
                         'async_timeout >= 1.3.0',
                         'aiohttp >= 1.2.0',
                         'numpy >= 1.8.0'],
-      test_suite='nose.collector',
-      tests_require=['nose >= 1.3.4', 'requests >= 2.4.3'],
+      #test_suite='nose.collector',
+      #tests_require=['nose >= 1.3.4', 'requests >= 2.4.3'],
 )
 
 # vim: fileencoding=utf-8 tabstop=4 shiftwidth=4 expandtab
